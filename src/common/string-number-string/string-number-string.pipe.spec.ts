import { StringNumberStringPipe } from './string-number-string.pipe';

describe('StringNumberStringPipe', () => {
  it('should be defined', () => {
    expect(new StringNumberStringPipe()).toBeDefined();
  });
});
